<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Z-Class 강의 생성</title>
        <link rel="stylesheet" href="/public/css/initial.css">
        <link rel="shortcut icon" href="/public/ico/favicon.ico" type="image/x-icon">
        <link rel="icon" href="/public/ico/favicon.ico" type="image/x-icon">
    </head>

    <body>
        
        <div id="background_container">
            <div id="background_1" class="background_base"></div>
            <div id="background_2" class="background_base"></div>
        </div>
        <form action="/course/make_course" method="post">
            <input type="text" name="title" placeholder="강의명" required="required">
            <button type="button" id="btn_add_course">시간 추가</button>
            <button type="button" id="btn_remove_course" disabled>시간 삭제</button>
            <!-- 수업 요일 & 시작 시간이 담김, 추가 / 삭제가 이 안에서 이루어짐 -->
            <div class="class_container">
                <!-- 수업 요일 -->
                <div id="day1_container">
                    <div class="day_container">
                        <label for="day1">수업 요일</label>
                        <select name="day1">
                            <option value="Sun.">일요일</option>
                            <option value="Mon.">월요일</option>
                            <option value="Tue.">화요일</option>
                            <option value="Wed.">수요일</option>
                            <option value="Thu.">목요일</option>
                            <option value="Fri.">금요일</option>
                            <option value="Sat.">토요일</option>
                        </select>
                    </div>
                    <!-- 수업 시작 시간 -->
                    <div class="time_container">
                        시작 시간<input type="time" name="time1" step="300" required="required">
                    </div>
                </div>
            </div>

            <!-- 선생님과 같은 소속인 학생들의 checkbox가 담김, 강의를 수강하는 학생들을 체크-->
            <div id="students_container">
                <!-- 학생들의 checkbox를 ejs로 생성 -->
                <% for (let i=0; i < students.length; i++ ){ %>
                    <input type="checkbox" name="students" value="<%=students[i]._id%>">
                    <label for="<%=students[i].id%>"><%=students[i].name%>,
                        <%=students[i].grade%>학년</label>
                <% } %>
            </div>
            <button type="button" id="btn_back" onclick="location.href='/';">취소</button>
            <button type="submit" id="btn_submit">생성</button>
        </form>

        <script src="/public/js/2_waiting_room/make_course.js"></script>
    </body>

</html>